# Nony Fitness & Nutrition
Single-file RTL Arabic fitness/nutrition dashboard.

## GitHub Pages
1. Upload `index.html` to the repository root.
2. GitHub → Settings → Pages.
3. Deploy from the `main` branch and `/root`.
4. Open the Pages URL.

The app is client-side only and stores account/demo data in LocalStorage.
